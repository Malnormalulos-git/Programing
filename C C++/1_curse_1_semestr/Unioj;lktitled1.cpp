#include <vector>
#include <iostream>

using namespace std;
int main(){	
vector<int> tail = vector<int>();

tail.push_back(1);

tail.push_back(1);
tail.push_back(2);
tail.push_back(5);

tail.inser

for(int i = 0; i< tail.size(); i++){
cout<<tail[i]<<endl;
}
return 0;
}

